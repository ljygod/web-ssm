<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.heitian.ssm.dao.UserMapper" >
  <resultMap id="BaseResultMap" type="com.heitian.ssm.model.User" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <result column="use_id" property="use_Id" jdbcType="VARCHAR" />
    <result column="user_password" property="user_Password" jdbcType="VARCHAR" />
    <result column="user_auth" property="user_Auth" jdbcType="VARCHAR" />
  </resultMap>
  <insert id="addUser">
    INSERT INTO user(use_id, user_password, user_auth) VALUES (#{user_id},#{user_password},#{user_auth})
  </insert>
    <update id="updateUser">
        UPDATE user SET user_password = #{password} where use_id = #{user_id}
    </update>
    <delete id="delUser">
    DELETE FROM user WHERE use_id=#{user_id}
  </delete>

  <select id="selectUserById" resultType="com.heitian.ssm.model.User" parameterType="string">
    SELECT * FROM user WHERE use_id = #{user_id}
  </select>
    <select id="showAllUser" resultType="com.heitian.ssm.model.User">
      SELECT * from user
    </select>
</mapper>